<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ComsciFoodAp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/questa</item>
        <item name="android:statusBarColor">@color/rose</item>
    </style>

    <style name="Theme.ComsciFoodAp" parent="Base.Theme.ComsciFoodAp" />

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Customize positive button -->
        <item name="colorAccent">@color/black</item>

        <!-- Customize negative button -->
        <item name="android:buttonBarButtonStyle">@style/NegativeButtonStyle</item>
    </style>


    <style name="NegativeButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/black</item>
    </style>

</resources>